Read `AGENTS.md`.

Implement TASK-014 — Technical Documents workspace.

Goal: turn the read-only Documents section from TASK-013 into the first editable business workspace using the existing TASK-008 schema.

Required routes:
- `/app/projects/[projectId]/documents`
- `/app/projects/[projectId]/documents/new`
- `/app/projects/[projectId]/documents/[documentId]`
- `/app/projects/[projectId]/documents/[documentId]/revisions/new`

The user must be able to:
- view real TechnicalDocuments;
- search by code/title;
- filter by revision status;
- open document details;
- view revision history;
- view IssueForWork history read-only;
- create TechnicalDocument;
- create a new DRAFT DocumentRevision.

Before implementation inspect:
- `docs/product/roles-permissions.md`
- `docs/product/workflows.md`
- `docs/product/domain-model.md`
- `docs/architecture/ARCHITECTURE.md`
- TASK-008 migration/tests
- TASK-012 Server Actions
- TASK-013 application shell
- current RLS and permission catalog.

Reuse existing code before creating new components, queries, helpers or actions.

Use Server Components by default.

For document creation use the existing `documents.create` permission with exact scope.

Flow:
Form → Server Action → Zod → trusted authenticated user/project → normal server Supabase client → RLS/constraints → redirect.

Minimum document fields:
- `code`
- `title`

Do not let the browser control:
- project_id
- created_by
- timestamps.

For revision creation use existing `documents.revision.create`.

Minimum revision input:
- `revision_code`

New revision must start in the approved initial state, normally DRAFT. Do not add a status selector and do not expose arbitrary lifecycle mutation.

Use only existing permission keys. Do not create new permission keys or grants.

Do not authorize by role name.

`documents.issue_for_work` remains outside this task. Do not add buttons/actions for issuing or withdrawing documents even if the permission exists.

Do not add:
- new business tables;
- new migration unless a genuine blocking schema/security defect is found;
- files or Storage;
- uploads;
- approval workflow;
- DocumentWorkLink management;
- DocumentImpact management;
- runtime service-role;
- global state libraries;
- generic repositories;
- generic forms/workflow engines.

Documents list must show real DB data:
- code;
- title;
- latest revision code/status;
- current IssueForWork state if available.

Derive latest/current revision state from normalized tables. Do not add `current_revision_id`.

Search/filtering must be server-side and preferably URL-driven:
- `search`
- `status`

Do not load all documents and filter client-side.

Document details page must show:
- document information;
- chronological revision history;
- IssueForWork history.

No IssueForWork mutation controls.

Handle errors safely in Russian:
- duplicate document code;
- duplicate revision code;
- invalid fields;
- unauthorized project;
- missing document;
- RLS denial;
- unexpected DB errors.

Do not render raw Supabase/Postgres errors.

Maintain the TASK-013 mobile-first UI style.

Reuse the existing demo:
- `pnpm demo:seed`
- login `demo@construction.test`
- password `Demo-Task012-2026!`

Do not add another demo seed or new grants.

Add focused tests for:
- document form validation;
- revision validation;
- safe duplicate errors.

Add Playwright flow:
login → project → Documents → create document → open it → create DRAFT revision → verify history → reload → persisted.

Also verify:
- duplicate document code gives safe error;
- duplicate revision code gives safe error;
- existing IssueForWork is visible but has no mutation controls;
- random/cross-project document URL exposes no data.

Run on Node 22.x:

pnpm db:start
pnpm db:reset
pnpm demo:seed
pnpm db:test
pnpm db:types
pnpm lint
pnpm typecheck
pnpm format:check
pnpm test
pnpm build
pnpm test:e2e
git diff --check

Inspect `git status` and `git diff`.

Expected DB result:
`Новых миграций нет.`

The final completion report MUST be entirely in Russian and include:

## Реализовано
## Маршруты
## Что можно делать
## Демо-доступ
## Что проверить руками
## Permission mapping
## Проверки
## Безопасность
## Изменения БД
## Что пока не реализовано

In `Демо-доступ`, print:

Адрес входа: http://localhost:3000/login
Логин: demo@construction.test
Пароль: Demo-Task012-2026!

And startup commands:

pnpm db:start
pnpm db:reset
pnpm demo:seed
pnpm dev

In `Что пока не реализовано`, explicitly list:
- загрузка файлов;
- approval lifecycle;
- IssueForWork action;
- управление связью document→Work.

Stop after TASK-014. Do not begin Work workspace, DocumentWorkLink management or IssueForWork controls.